# Copy to .env (gitignored). `docker compose` reads .env automatically and passes
# these into the live-demos container. (You can also skip this and choose the
# provider + paste the key in the app sidebar instead.)

# Chat provider: "openai" (default) or "anthropic"
LLM_PROVIDER=openai

# Chat key — set the one matching LLM_PROVIDER:
OPENAI_API_KEY=sk-proj-your-key-here
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# Embeddings are ALWAYS OpenAI (Anthropic has no embeddings API), so the RAG
# demos need OPENAI_API_KEY set even when LLM_PROVIDER=anthropic.
